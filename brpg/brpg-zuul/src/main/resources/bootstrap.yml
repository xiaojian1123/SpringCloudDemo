server:
  port: 1020
spring:
  application:
    name: brpg-zuul
  cloud:
    config:
#      uri: http://192.168.0.124:1010/  #配置中心单服务开放
      discovery:  #配置中心集群开放
        enabled: true
        service-id: brpg-config
      profile: default
      #git 加上master    svn  则不用写
      label:

eureka:
  instance:
    #优先使用IP地址作为主机名的标识
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://192.168.0.124:1000/eureka,http://192.168.0.118:1000/eureka